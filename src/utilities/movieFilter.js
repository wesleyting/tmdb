//probably use this to read or store the entire list/collection returned by API
//maybe use another file called movieParse to retrieve individual information?
//api -> (list object) -> movieFilter.js -> (individual movie objects) -> movieParse.js

import React from 'react';



export default movieFilter;